package ejercicio1;

public class ejemploMetodo {
	/*
	 * Palabras clave: funciones, variables locales.
	 * 
	 * Metodos como comportamientos
	 * Funciones que se declaran dentro de una clase
	 * 
	 * class NombreClase {
	 * 		// atrbts declaración
	 * 
	 *  	// metds declaración
	 * 
	 * 		Si devuelve algo hay que poner el tipo, sino ponemos void, si
	 * 		fuese entener pondríamos int			
	 * 
	 * 		NombreMetodo(){ Puede ser boolean, puede devolver un tipo objeto
	 * 
	 * 		}
	 * 
	 * class Avion{
	 * 
	 * 	int cantidadCombustible= 1000;
	 *  int alturaVuelo = 1;
	 *		 
	 * 	boolean despegar(int cantidadCombustible,int alturaVuelo ){
	 * 
	 * 	this.cantidadCombustible -= cantidadCombustible;
	 *  this.alturaVuelo += alturaVuelo;
	 * 	if(this.cantidadCombustible>0){
	 * 			
	 * 		 return (this.cantidadCombustible > 0); // True si cantcombus es >0 o false si es <0 
	 *   }
	 *   
	 *   boolean despegue= avion1.despegar(100,20000);
	 *   System.out.println(despegue);
	 * 
	 */ 		
	
	char letra;
	boolean esVocal(char letra){
		if((letra == a) || (letra == e) ||(letra == i) || (letra == o) || (letra=u) ) {
		return true;
		System.out.println("Es vocal");
		}
		
		
		
		
		
	}

}
